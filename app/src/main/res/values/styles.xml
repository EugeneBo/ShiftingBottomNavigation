<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@android:color/darker_gray</item>
    </style>

    <style name="SearchView">
        <item name="floatingSearch_viewSearchInputTextColor">@android:color/white</item>
        <item name="floatingSearch_hintTextColor">@android:color/darker_gray</item>
        <item name="floatingSearch_leftActionColor">@android:color/white</item>
        <item name="floatingSearch_viewSuggestionItemTextColor">@android:color/white</item>
        <item name="floatingSearch_dividerColor">@android:color/white</item>
        <item name="floatingSearch_clearBtnColor">@android:color/white</item>
        <item name="floatingSearch_menuItemIconColor">@android:color/white</item>
        <item name="floatingSearch_suggestionRightIconColor">@android:color/white</item>

    </style>

</resources>
